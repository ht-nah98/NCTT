# Script đã loại bỏ

Giữ lại để truy vết, **không nằm trong pipeline**. Không tham chiếu từ tài liệu nào.

| File | Vì sao bỏ |
|---|---|
| `step04e_thumbnail_dna.py` | Bản OCR đầu tiên. Bị `step04g_brief_extract.py` thay thế (04g đo cả NGƯỜI bằng YOLO, 04e chỉ có chữ + màu). |
| `step04f_thumbnail_v2.py` | Bản đo lại toàn ngách. Không cần: brief chỉ dùng nhóm top 5%, quét cả 6.669 ảnh tốn 2 giờ mà không thêm kết luận nào. |

Xóa an toàn nếu không cần đối chiếu lịch sử.
